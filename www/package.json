{
  "name": "herb-game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "lint": "tsc",
    "dev": "pnpm wasm && vite",
    "build": "pnpm wasm && vite build",
    "wasm": "cd .. && cargo build --target wasm32-unknown-unknown && wasm-bindgen target/wasm32-unknown-unknown/debug/herb_game.wasm --out-dir www/wasm-build --target web && cd www"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.6.0"
  }
}
